plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
    id "com.google.gms.google-services"
    id "com.google.firebase.crashlytics"
}

android {
    // تحديد namespace بشكل صريح (إلزامي في Gradle 8+)
    namespace "com.totv.plus"
    compileSdk 34
    ndkVersion "26.1.10909125"

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = '17'
    }

    defaultConfig {
        applicationId "com.totv.plus"
        minSdk 24
        targetSdk 34
        versionCode 2
        versionName "2.0.0"
        
        multiDexEnabled true
        
        // تحسينات الأداء
        vectorDrawables.useSupportLibrary = true
        
        // دعم Hardware Decoder للفيديو 4K
        ndk {
            abiFilters 'armeabi-v7a', 'arm64-v8a', 'x86', 'x86_64'
        }
        
        // تفعيل Renderscript للمعالجة السريعة
        renderscriptTargetApi 34
        renderscriptSupportModeEnabled true
    }

    buildTypes {
        release {
            // R8 Full Mode لضغط الكود وحماية روابط البث
            minifyEnabled true
            shrinkResources true
            
            // استخدام R8 بدلاً من ProGuard
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            
            // تحسينات إضافية
            ndk {
                debugSymbolLevel 'FULL'
            }
            
            // إزالة الموارد غير المستخدمة
            resValue "string", "app_name", "ToTV+"
        }
        
        debug {
            minifyEnabled false
            debuggable true
            applicationIdSuffix ".debug"
            versionNameSuffix "-DEBUG"
            resValue "string", "app_name", "ToTV+ Debug"
        }
    }

    buildFeatures {
        viewBinding true
        buildConfig true
    }

    packaging {
        resources {
            excludes += [
                'META-INF/DEPENDENCIES',
                'META-INF/LICENSE',
                'META-INF/LICENSE.txt',
                'META-INF/license.txt',
                'META-INF/NOTICE',
                'META-INF/NOTICE.txt',
                'META-INF/notice.txt',
                'META-INF/ASL2.0',
                'META-INF/*.kotlin_module'
            ]
        }
    }

    lint {
        disable 'InvalidPackage'
        checkReleaseBuilds false
    }
}

flutter {
    source '../..'
}

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk8:$kotlin_version"
    
    // Firebase BoM للتوافق التلقائي
    implementation platform('com.google.firebase:firebase-bom:33.1.0')
    implementation 'com.google.firebase:firebase-analytics-ktx'
    implementation 'com.google.firebase:firebase-config-ktx'
    implementation 'com.google.firebase:firebase-crashlytics-ktx'
    
    // MultiDex
    implementation 'androidx.multidex:multidex:2.0.1'
    
    // AndroidX Core (للتوافق)
    implementation 'androidx.core:core-ktx:1.13.1'
    implementation 'androidx.appcompat:appcompat:1.7.0'
    
    // ExoPlayer للفيديو (يستخدمه media_kit تلقائياً)
    implementation 'androidx.media3:media3-exoplayer:1.3.1'
    implementation 'androidx.media3:media3-exoplayer-hls:1.3.1'
    implementation 'androidx.media3:media3-exoplayer-dash:1.3.1'
    implementation 'androidx.media3:media3-ui:1.3.1'
}
